# File: requirements.txt
# Dependensi yang dibutuhkan oleh skrip orkestrasi Python.

# Required for secret encryption when communicating with GitHub API
PyNaCl>=1.5.0

# Optional: Progress bars for better UX (uncomment if needed)
# tqdm>=4.66.0

# Optional: Secure credential storage (uncomment if needed)
# keyring>=24.0.0

# Optional: Better retry logic with exponential backoff (uncomment if needed)
# tenacity>=8.2.0
